import { io } from 'socket.io-client'

export default defineNuxtPlugin(() => {
  // let { userData } = userAuth()
  // let config = useRuntimeConfig()

  // const socket = io(config.public.API_BASE_URL, {
  //   query: { userId: userData?.value?._id }
  // })
})
